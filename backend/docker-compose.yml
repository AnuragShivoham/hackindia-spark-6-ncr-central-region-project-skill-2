version: '3.8'

services:
  api:
    build: .
    container_name: amit-bodhit-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./workspace:/app/workspace
